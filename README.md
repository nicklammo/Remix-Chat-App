A simple chat app built from scratch, with the exception of react-resize library being used in one instance. Relies on context API heavily and makes calls to Remix API routes to sync to database (Prisma).
